// Utilizando o FOR para percorrer o Length da UL e aplicar cor para todas as LIs

let lista = document.getElementsByClassName('items')

for (let contador = 0; contador <= lista.length; contador++ ) {
    lista[contador].style.color = 'red'
    lista[contador].style.backgroundColor = 'silver'
}


// Calculando média


