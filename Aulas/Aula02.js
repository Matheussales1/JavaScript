var nome = "Matheus" 
var sobren = "Sales"

// window.alert(`Olá, ${nome} ${sobren}`)

var nota1 = 6
var nota2 = 10
var media = (nota1 + nota2) / 2
var passou = media >= 7

console.log(`O aluno ${nome} ${sobren} teve a média de ${media}. | Passou?: ${passou}`)
